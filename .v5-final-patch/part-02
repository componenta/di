 const int FORMAT_VERSION = 5;
+    public const int FORMAT_VERSION = 6;
 
     public static function calculate(
         AttributeDefinitionRegistry $attributes,
diff --git a/src/Compile/Factory/CompiledFactoryShardCompiler.php b/src/Compile/Factory/CompiledFactoryShardCompiler.php
index 2136a45..6771fb4 100644
--- a/src/Compile/Factory/CompiledFactoryShardCompiler.php
+++ b/src/Compile/Factory/CompiledFactoryShardCompiler.php
@@ -10,7 +10,7 @@ use InvalidArgumentException;
 /** Packs generated entry methods into immutable content-addressed shards. */
 final readonly class CompiledFactoryShardCompiler
 {
-    public const int FORMAT_VERSION = 4;
+    public const int FORMAT_VERSION = 5;
     public const int DEFAULT_MAX_BYTES = 131072;
     public const string FILE_PREFIX = 'container.factories.';
 
@@ -81,7 +81,7 @@ final readonly class CompiledFactoryShardCompiler
         ));
         $id = substr(hash('sha256', self::FORMAT_VERSION . "\0" . $namespace . "\0" . $payload), 0, 32);
         $class = 'CompiledFactoryShard_' . $id;
-        $code = $this->code($namespace, $class, $payload);
+        $code = $this->code($namespace, $class, $payload, $shard);
         $file = self::FILE_PREFIX . substr(hash('sha256', $code), 0, 32) . '.php';
         $this->writer->write(rtrim($directory, '/\\') . DIRECTORY_SEPARATOR . $file, $code);
 
@@ -96,8 +96,18 @@ final readonly class CompiledFactoryShardCompiler
         }
     }
 
-    private function code(string $namespace, string $class, string $methods): string
+    /** @param list<GeneratedFactory> $factories */
+    private function code(string $namespace, string $class, string $methods, array $factories): string
     {
+        $plans = implode(",\n", array_map(
+            static fn(GeneratedFactory $factory): string => sprintf(
+                '            \\%s::class => $objects->prepare(\\%s::class)',
+                $factory->class,
+                $factory->class,
+            ),
+            $factories,
+        ));
+
         return sprintf(
             <<<'PHP'
 <?php
@@ -110,7 +120,15 @@ final class %s
 {
     public const string PIPELINE_FINGERPRINT = %s;
 
-    public function __construct(private readonly \%s $objects) {}
+    /** @var array<class-string, \%s> */
+    private readonly array $plans;
+
+    public function __construct(private readonly \%s $objects)
+    {
+        $this->plans = [
+%s
+        ];
+    }
 
 %s
 }
@@ -120,7 +138,9 @@ PHP,
             $namespace,
             $class,
             var_export($this->pipelineFingerprint, true),
+            \Componenta\DI\Object\ObjectPlan::class,
             ObjectPipeline::class,
+            $plans,
             self::indent($methods, 4),
             $class,
         );
diff --git a/src/Compile/Factory/FactoryCodeGenerator.php b/src/Compile/Factory/FactoryCodeGenerator.php
index c2da785..cf231fc 100644
--- a/src/Compile/Factory/FactoryCodeGenerator.php
+++ b/src/Compile/Factory/FactoryCodeGenerator.php
@@ -32,7 +32,7 @@ final readonly class FactoryCodeGenerator
             <<<'PHP'
 public function %s(\%s $context): object
 {
-    return $this->objects->create(%s::class, $context);
+    return $this->objects->createPrepared($this->plans[%s::class], $context);
 }
 PHP,
             $method,
diff --git a/src/ContainerBuilder.php b/src/ContainerBuilder.php
index 77453ec..1c65e93 100644
--- a/src/ContainerBuilder.php
+++ b/src/ContainerBuilder.php
@@ -15,6 +15,7 @@ use Componenta\DI\Attribute\Composition\Capability\ConstructorPolicy;
 use Componenta\DI\Attribute\Composition\Capability\CreationStrategy;
 use Componenta\DI\Attribute\Composition\Capability\LifecycleHook;
 use Componenta\DI\Attribute\Composition\Capability\ValueProvider;
+use Componenta\DI\Attribute\Composition\Capability\ValueProviderDecorator;
 use Componenta\DI\Attribute\Composition\Capability\ValueTransformer;
 use Componenta\DI\Attribute\Composition\CapabilityPolicy;
 use Componenta\DI\Attribute\Config as ConfigAttribute;
@@ -35,6 +36,7 @@ use Componenta\DI\Attribute\Handler\Builtin\NoConstructorPolicyHandler;
 use Componenta\DI\Attribute\Handler\Builtin\ProxyCreationHandler;
 use Componenta\DI\Attribute\Handler\Builtin\RequestValueProvider;
 use Componenta\DI\Attribute\Handler\Builtin\SetUpLifecycleHandler;
+use Componenta\DI\Attribute\Handler\Builtin\VirtualProxyValueDecorator;
 use Componenta\DI\Attribute\Header;
 use Componenta\DI\Attribute\Init;
 use Componenta\DI\Attribute\Inject;
@@ -49,6 +51,7 @@ use Componenta\DI\Attribute\RequestAttribute;
 use Componenta\DI\Attribute\ServerParam;
 use Componenta\DI\Attribute\SetUp;
 use Componenta\DI\Attribute\UploadedFile;
+use Componenta\DI\Attribute\VirtualProxy;
 use Componenta\DI\Compile\Autowire\AutowireClassGraph;
 use Componenta\DI\Compile\Autowire\AutowireEntry;
 use Componenta\DI\Compile\Factory\CompiledFactoryDefinition;
@@ -65,7 +68,6 @@ use Componenta\DI\Resolver\Entry\CompositeResolver;
 use Componenta\DI\Resolver\Entry\EntryResolverInterface;
 use Componenta\DI\Resolver\Entry\FactoryResolver as EntryFactoryResolver;
 use Componenta\DI\Resolver\Entry\FactorySpecificationValidator;
-use Componenta\DI\Resolver\Entry\InstanceCreator;
 use Componenta\DI\Resolver\Entry\InvokableResolver;
 use Componenta\DI\Resolver\Entry\ReflectionResolver;
 use Componenta\DI\Resolver\Parameter\ParametersResolver;
@@ -89,7 +91,7 @@ use ReflectionClass;
  */
 class ContainerBuilder
 {
-    public const int CACHE_VERSION = 12;
+    public const int CACHE_VERSION = 13;
 
     /** @var array<string, non-empty-string> */
     private const array DEFAULT_ALIASES = [
@@ -174,7 +176,7 @@ class ContainerBuilder
         $values = new ValuePipeline($fallbacks);
         $parameters = new ParametersResolver($plans, $values);
         $proxyFactory = $this->createProxyFactory();
-        $objects = new ObjectPipeline($plans, new InstanceCreator($parameters), $values, $proxyFactory);
+        $objects = new ObjectPipeline($plans, $parameters, $values, $proxyFactory);
 
         $aliases = new AliasResolver([...$this->aliases, ConfigAttribute::KEY => Config::class]);
         $cache = new EntryCache();
@@ -235,7 +237,7 @@ class ContainerBuilder
         );
         $bootstrap->initialize($entryResolver, $executor);
 
-        $this->registerBuiltInAttributes($attributes, $container, $executor);
+        $this->registerBuiltInAttributes($attributes, $container, $executor, $proxyFactory);
         $this->registerBuiltInFallbacks($fallbacks, $container);
 
         foreach ($this->attributeCapabilities as $policy) {
@@ -273,9 +275,11 @@ class ContainerBuilder
     ): array {
         $container = $this->build();
         $objects = $container->get(ObjectPipeline::class);
+        $plans = $container->get(AttributePlanBuilder::class);
         $attributes = $container->get(AttributeDefinitionRegistry::class);
         $fallbacks = $container->get(ValueFallbackRegistry::class);
         if (!$objects instanceof ObjectPipeline
+            || !$plans instanceof AttributePlanBuilder
             || !$attributes instanceof AttributeDefinitionRegistry
             || !$fallbacks instanceof ValueFallbackRegistry
         ) {
@@ -288,7 +292,7 @@ class ContainerBuilder
             $excluded[$aliasResolver->resolve($id)] = true;
         }
 
-        $classes = (new AutowireClassGraph($this->aliases))->expand($entries, $excluded);
+        $classes = (new AutowireClassGraph($this->aliases, $plans))->expand($entries, $excluded);
         if ($classes === []) {
             return [];
         }
@@ -345,9 +349,11 @@ class ContainerBuilder
         AttributeDefinitionRegistry $registry,
         Container $container,
         CallableExecutorInterface $executor,
+        ProxyFactoryInterface $proxyFactory,
     ): void {
         foreach ([
             new CapabilityPolicy(ValueProvider::class, 1),
+            new CapabilityPolicy(ValueProviderDecorator::class),
             new CapabilityPolicy(ValueTransformer::class),
             new CapabilityPolicy(CreationStrategy::class, 1),
             new CapabilityPolicy(ConstructorPolicy::class, 1),
@@ -378,6 +384,7 @@ class ContainerBuilder
             $registry->register(new AttributeDefinition($attribute, $handler, [ValueProvider::class]));
         }
 
+        $registry->register(new AttributeDefinition(VirtualProxy::class, new VirtualProxyValueDecorator($proxyFactory), [ValueProviderDecorator::class]));
         $registry->register(new AttributeDefinition(Cast::class, new CastValueTransformer($container), [ValueTransformer::class]));
         $registry->register(new AttributeDefinition(Lazy::class, new LazyCreationHandler(), [CreationStrategy::class]));
         $registry->register(new AttributeDefinition(Proxy::class, new ProxyCreationHandler(), [CreationStrategy::class]));
diff --git a/src/Definition/Definition.php b/src/Definition/Definition.php
index 5bbb319..b29dc31 100644
--- a/src/Definition/Definition.php
+++ b/src/Definition/Definition.php
@@ -8,7 +8,7 @@ namespace Componenta\DI\Definition;
 final class Definition
 {
     /**
-     * @param callable(\Componenta\Config\ContainerValue, array<string|int, mixed>):mixed $factory
+     * @param callable(\Componenta\Config\ContainerValue, \Componenta\DI\ResolutionContext):mixed $factory
      */
     public static function factory(callable $factory): FactoryDefinition
     {
diff --git a/src/Definition/FactoryDefinition.php b/src/Definition/FactoryDefinition.php
index d812219..dc88f82 100644
--- a/src/Definition/FactoryDefinition.php
+++ b/src/Definition/FactoryDefinition.php
@@ -15,12 +15,12 @@ namespace Componenta\DI\Definition;
 final readonly class FactoryDefinition implements DefinitionInterface
 {
     /**
-     * @var callable(\Componenta\Config\ContainerValue, array<string|int, mixed>):mixed
+     * @var callable(\Componenta\Config\ContainerValue, \Componenta\DI\ResolutionContext):mixed
      */
     public mixed $value;
 
     /**
-     * @param callable(\Componenta\Config\ContainerValue, array<string|int, mixed>):mixed $value
+     * @param callable(\Componenta\Config\ContainerValue, \Componenta\DI\ResolutionContext):mixed $value
      */
     public function __construct(callable $value)
     {
diff --git a/src/Object/ObjectPipeline.php b/src/Object/ObjectPipeline.php
index cc6ea5d..abfaec4 100644
--- a/src/Object/ObjectPipeline.php
+++ b/src/Object/ObjectPipeline.php
@@ -4,7 +4,6 @@ declare(strict_types=1);
 
 namespace Componenta\DI\Object;
 
-use Componenta\DI\Attribute\Composition\AttributePlan;
 use Componenta\DI\Attribute\Composition\AttributePlanBuilder;
 use Componenta\DI\Attribute\Composition\Capability\ConstructorPolicy;
 use Componenta\DI\Attribute\Composition\Capability\CreationStrategy as CreationStrategyCapability;
@@ -17,7 +16,7 @@ use Componenta\DI\Attribute\Handler\LifecycleHookHandlerInterface;
 use Componenta\DI\Exception\ResolutionException;
 use Componenta\DI\ProxyFactoryInterface;
 use Componenta\DI\ResolutionContext;
-use Componenta\DI\Resolver\Entry\InstanceCreator;
+use Componenta\DI\Resolver\Parameter\ParametersResolver;
 use Componenta\DI\Resolver\Target\PropertyTarget;
 use Componenta\DI\Value\ValueContext;
 use Componenta\DI\Value\ValuePipeline;
@@ -25,17 +24,17 @@ use LogicException;
 use ReflectionClass;
 use ReflectionProperty;
 
-/** Single runtime object-creation pipeline shared by reflection and compiled entries. */
+/** Single warm object-creation pipeline shared by reflection and compiled entries. */
 final class ObjectPipeline
 {
     /** @var array<class-string, ObjectPlan> */
-    private array $objectPlans = [];
+    private array $cache = [];
 
     public function __construct(
-        private AttributePlanBuilder $plans,
-        private InstanceCreator $instances,
-        private ValuePipeline $values,
-        private ProxyFactoryInterface $proxies,
+        private readonly AttributePlanBuilder $plans,
+        private readonly ParametersResolver $parameters,
+        private readonly ValuePipeline $values,
+        private readonly ProxyFactoryInterface $proxies,
     ) {}
 
     /** @param class-string $class */
@@ -43,75 +42,149 @@ final class ObjectPipeline
         string $class,
         ResolutionContext $context = new ResolutionContext(),
     ): object {
-        $objectPlan = $this->objectPlan($class);
-        $reflection = $objectPlan->class;
-        $plan = $objectPlan->attributes;
-        $useConstructor = $this->useConstructor($reflection, $plan, $context);
-        $strategy = $this->strategy($reflection, $plan, $context);
+        return $this->createPrepared($this->prepare($class), $context);
+    }
+
+    /** @param class-string $class */
+    public function prepare(string $class): ObjectPlan
+    {
+        if (isset($this->cache[$class])) {
+            return $this->cache[$class];
+        }
+
+        /** @var ReflectionClass<object> $reflection */
+        $reflection = new ReflectionClass($class);
+        /** @var class-string $resolvedClass */
+        $resolvedClass = $reflection->getName();
+        if (isset($this->cache[$resolvedClass])) {
+            return $this->cache[$class] = $this->cache[$resolvedClass];
+        }
+
+        $constructor = $reflection->getConstructor();
+        $constructorTargets = $constructor === null
+            ? []
+            : $this->parameters->plans(array_values($constructor->getParameters()));
+        $properties = [];
+
+        foreach (self::properties($reflection) as $property) {
+            $attributes = $this->plans->build($property);
+            if (!$attributes->has(ValueProvider::class) && !$attributes->has(ValueTransformer::class)) {
+                continue;
+            }
+
+            $properties[] = new PropertyValuePlan(
+                $property,
+                new PropertyTarget($property),
+                $attributes,
+            );
+        }
+
+        $plan = new ObjectPlan(
+            $reflection,
+            $this->plans->build($reflection),
+            $constructor,
+            $constructorTargets,
+            $properties,
+        );
+
+        $this->cache[$resolvedClass] = $plan;
+        if ($class !== $resolvedClass) {
+            $this->cache[$class] = $plan;
+        }
+
+        return $plan;
+    }
+
+    public function createPrepared(
+        ObjectPlan $plan,
+        ResolutionContext $context = new ResolutionContext(),
+    ): object {
+        $useConstructor = $this->useConstructor($plan, $context);
+        $strategy = $this->strategy($plan, $context);
 
         return match ($strategy) {
-            CreationStrategy::Eager => $this->eager($objectPlan, $context, $useConstructor),
-            CreationStrategy::Lazy => $this->lazy($objectPlan, $context, $useConstructor),
-            CreationStrategy::Proxy => $this->proxy($objectPlan, $context, $useConstructor),
+            CreationStrategy::Eager => $this->eager($plan, $context, $useConstructor),
+            CreationStrategy::Lazy => $this->lazy($plan, $context, $useConstructor),
+            CreationStrategy::Proxy => $this->proxy($plan, $context, $useConstructor),
         };
     }
 
     private function eager(
-        ObjectPlan $objectPlan,
+        ObjectPlan $plan,
         ResolutionContext $context,
         bool $useConstructor,
     ): object {
-        $class = $objectPlan->class;
         $entry = $useConstructor
-            ? $this->instances->create($class, $context)
-            : $class->newInstanceWithoutConstructor();
+            ? $this->instantiate($plan, $context)
+            : $plan->reflection->newInstanceWithoutConstructor();
 
-        $this->populate($entry, $objectPlan, $context);
-        $this->lifecycle($entry, $class, $objectPlan->attributes, $context);
+        $this->populate($entry, $plan, $context, $useConstructor);
+        $this->lifecycle($entry, $plan, $context);
 
         return $entry;
     }
 
     private function lazy(
-        ObjectPlan $objectPlan,
+        ObjectPlan $plan,
         ResolutionContext $context,
         bool $useConstructor,
     ): object {
-        $class = $objectPlan->class;
-
         return $this->proxies->makeLazy(
-            $class->getName(),
-            function (object $entry) use ($objectPlan, $class, $context, $useConstructor): void {
+            $plan->reflection->getName(),
+            function (object $entry) use ($plan, $context, $useConstructor): void {
                 if ($useConstructor) {
-                    $this->instances->initialize($entry, $class, $context);
+                    $this->initialize($entry, $plan, $context);
                 }
 
-                $this->populate($entry, $objectPlan, $context);
-                $this->lifecycle($entry, $class, $objectPlan->attributes, $context);
+                $this->populate($entry, $plan, $context, $useConstructor);
+                $this->lifecycle($entry, $plan, $context);
             },
         );
     }
 
     private function proxy(
-        ObjectPlan $objectPlan,
+        ObjectPlan $plan,
         ResolutionContext $context,
         bool $useConstructor,
     ): object {
-        $class = $objectPlan->class;
-
         return $this->proxies->makeProxy(
-            $class->getName(),
-            fn(object $_proxy): object => $this->eager($objectPlan, $context, $useConstructor),
+            $plan->reflection->getName(),
+            fn(object $_proxy): object => $this->eager($plan, $context, $useConstructor),
         );
     }
 
-    /** @param ReflectionClass<object> $class */
-    private function useConstructor(
-        ReflectionClass $class,
-        AttributePlan $plan,
-        ResolutionContext $context,
-    ): bool {
-        $usage = $plan->one(ConstructorPolicy::class);
+    private function instantiate(ObjectPlan $plan, ResolutionContext $context): object
+    {
+        if ($plan->constructor === null) {
+            return $plan->reflection->newInstance();
+        }
+
+        $arguments = $this->parameters->resolvePlans